# Dependencies
node_modules/

# Build
dist/
*.tsbuildinfo

# IDE
.vscode/
.idea/
*.swp
*.swo
.DS_Store

# Logs
*.log
npm-debug.log*

# Environment
.env
.env.local

# Test
coverage/

# Cache (user-specific, legacy location — now lives in the OS cache dir via env-paths)
.mcdev-mcp/

# Decompiler artifacts (should live in the OS cache dir, see src/utils/paths.ts)
lib/__pycache__/
lib/mappings/
lib/src/
lib/tmp/
lib/versions/

# MCPB build output (produced by scripts/build-mcpb.sh and CI release workflow)
dist-mcpb/
*.mcpb

# MCPB bundles
*.mcpb
