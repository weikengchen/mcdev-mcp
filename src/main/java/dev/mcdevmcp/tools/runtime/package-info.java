/**
 * Concrete DebugBridge runtime tool handlers. The MCP factory composes these bindings while bridge
 * transport and session ownership remain in {@code dev.mcdevmcp.bridge}.
 */
package dev.mcdevmcp.tools.runtime;
